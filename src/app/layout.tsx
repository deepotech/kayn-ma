import './globals.css';

export const metadata = {
    title: 'Cayn.ma',
    description: 'Marketplace for cars in Morocco',
};

export default function RootLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
